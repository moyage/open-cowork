# Milestone 1 / Milestone 2 当前轮收口与剩余项

## 1. 目的

本文件用于回答两个问题：

1. 当前这一轮 `Milestone 1 + Milestone 2` 到底已经推进到了哪里；
2. 如果我们要把“这一轮”正式收口，还剩多少项值得继续做。

它不是新的需求文档，也不是下一轮路线图。  
它的作用是帮助我们在不扩张范围的前提下，判断当前轮是否已经接近完成。

## 2. 当前轮已完成部分

### 2.1 Milestone 1

已完成：

1. 复杂协作主链闭环
2. 最小边界硬化
3. 最小 continuity
4. 最小人类状态面
5. 最小诊断与恢复入口

### 2.2 Milestone 2

已完成：

1. 边界与状态第二轮硬化
2. runtime / timeline machine-readable 协议层
3. continuity primitives 最小完整链
4. closeout / sync / export 外层桥接
5. sync-history 的写入、查询、跨月聚合、分组摘要
6. digest 的轻量阅读与 projection 元数据

## 3. 当前轮还剩多少项

按“本轮最终收口”而不是“无限继续打磨”的标准看，本文件最初记录的剩余项为 **3 项**。

### R1. 公开状态口径收口

目标：

1. 更新公开状态报告
2. 确保仓库公开文档和当前主线实际能力一致

状态：

- 已完成

### R2. 当前轮 closeout 摘要固定

目标：

1. 固定“本轮完成了什么”
2. 固定“哪些事项明确留待下一轮”
3. 避免后续又靠会话记忆来判断边界

状态：

- 已完成

### R3. 最后一轮非扩张式阅读层压缩

目标：

1. 只在现有 `digest / sync-history / query` 上做很小的压缩
2. 不新增协议对象
3. 不新增新的主线能力包

说明：

- 该项已按“小切片、不扩张协议面”的原则完成

## 4. 哪些事明确不再纳入本轮

以下事项即使“看起来还能继续做”，也不应继续放在本轮里：

1. 新的 continuity 协议对象
2. 更重的 adapter contract 扩张
3. 生态级 sync / portfolio 管理
4. UI / dashboard / TUI 产品壳
5. 主导型 cowork agent persona

## 5. 建议的收口顺序

建议顺序：

1. 先完成 `R1`
2. 再完成 `R2`
3. 最后只在必要时做 `R3`

当前状态：

1. `R1` 已完成
2. `R2` 已完成
3. `R3` 已完成

因此，这一轮已经达到“可正式收口”的状态。

## 6. 一句话判断

当前这一轮并没有“还差很多大项”。  
如果按不扩张范围的标准来算，原先识别出的 3 项收口工作现已全部完成，因此当前轮已经可以正式视为收口完成。
