# Tasks

- Update generated Agent entry text and docs to avoid treating open-cowork as a platform Skill.
- Add collaborator and external-rule confirmation state and CLI commands.
- Add Step 1-4 artifact checks to execution readiness.
- Improve status output to show the new readiness fields.
- Make the smoke script skip unittest discovery when `tests/` is absent.
- Run syntax and smoke verification.
